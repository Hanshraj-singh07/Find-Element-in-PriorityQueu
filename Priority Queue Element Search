import java.util.PriorityQueue;

public class PriorityQueueSearch {
    public static void main(String[] args) {
        PriorityQueue<Integer> pq = new PriorityQueue<>();

        pq.add(10);
        pq.add(20);
        pq.add(15);
        pq.add(30);
        pq.add(5);

        int target = 15;
        int target = 15;
        if (pq.contains(target)) {
            System.out.println("Element " + target + " found in the PriorityQueue.");
        } else {
            System.out.println("Element " + target + " not found in the PriorityQueue.");
        }
    }
}
